local fs = _G.fs or {}

function fs.find(name)
	local out = {}

	return out
end

function fs.getcd()
	return "."
end

function fs.setcd(path)

end

function fs.createdir(path)

end

function fs.getattributes(path)
	return false
end

return fs
