// -jp=Fpa -
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "onsave",
            "type": "shell",
            "command": "cd ${workspaceFolder} && luajit -e \"assert(loadfile('.vscode/on_editor_save.lua'))('${file}','${workspaceFolder}')\""
        }
    ]
}