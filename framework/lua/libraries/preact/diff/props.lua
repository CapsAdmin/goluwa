function diffProps(dom, newProps, oldProps, isSvg, hydrate) end

function setStyle(style, key, value) end

function setProperty(dom, name, value, oldValue, isSvg) end

function eventProxy(e) end

function eventProxyCapture(e) end