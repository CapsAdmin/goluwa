local function diffChildren(
	parentDom,
	renderResult,
	newParentVNode,
	oldParentVNode,
	globalContext,
	isSvg,
	excessDomChildren,
	commitQueue,
	oldDom,
	isHydrating
) end

local function reorderChildren(childVNode, oldDom, parentDom) end

local function toChildArray(children, out) end

local function placeChild() end